function SieveOfEratosthenes()
{
  v = document.getElementById("eratostenes").value;
  arr = [];

  for(i = 0 ; i < v ; i++){
      arr.add([i+2, false]);
  }

  for(i = 0 ; i < arr.size ; i++){
    if(arr[i][0]%)
  }
  
  alert(v);
}
